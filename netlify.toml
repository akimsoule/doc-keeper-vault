[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "dist"

# Redirection générale API vers les fonctions Netlify
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
